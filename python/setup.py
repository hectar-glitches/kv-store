from setuptools import setup, find_packages

setup(
    name="kvstore_client",
    version="0.1.0",
    description="Python client for the KV-Store vector API",
    packages=find_packages(),
    python_requires=">=3.9",
)
